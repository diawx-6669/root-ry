# Локальная база разработки: скачанные бинарники PostgreSQL и его данные.
devtools/localdb/.pgdata/

# Скомпилированные бинарники
*.exe
rootry

# macOS
.DS_Store
*.exe~

# Документы научной работы и черновые выгрузки: большие docx/pdf/png,
# которые меняются целиком при каждой правке — git хранит их плохо,
# а история репозитория из-за них растёт быстрее самого кода.
Документы РКНП/
Claude outputs/

# Черновик текста коммита: нужен только чтобы кириллица не поехала
# в терминале Windows, в историю ему не надо.
commit-message.txt
